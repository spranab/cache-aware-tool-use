% Header for paper/PAPER.md → PAPER.pdf via pandoc + xelatex.
% Loaded via `--include-in-header paper/header.tex`.

\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,calc,decorations.pathreplacing,fit,backgrounds}

\tikzset{
  promptbox/.style={
    draw, rounded corners=2pt,
    minimum height=0.75cm,
    font=\small\ttfamily,
    inner sep=4pt,
    align=center,
  },
  cached/.style={
    promptbox,
    fill=green!18,
    draw=green!55!black,
    line width=0.5pt,
  },
  dynbox/.style={
    promptbox,
    fill=orange!18,
    draw=orange!70!black,
    line width=0.5pt,
  },
  neutral/.style={
    promptbox,
    fill=gray!12,
    draw=gray!60!black,
    line width=0.5pt,
  },
  llmbox/.style={
    draw, rounded corners=4pt,
    minimum height=0.9cm,
    font=\small\sffamily,
    align=center,
    inner sep=7pt,
    fill=blue!7,
    draw=blue!50!black,
    line width=0.6pt,
  },
  brokerbox/.style={
    llmbox,
    fill=violet!8,
    draw=violet!55!black,
  },
  retrieverbox/.style={
    llmbox,
    fill=teal!8,
    draw=teal!55!black,
  },
  flowarrow/.style={-Stealth, line width=0.7pt},
  bracelabel/.style={font=\scriptsize\sffamily},
}
