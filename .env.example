# Provider: deepseek (default, cheap) | openai
PROVIDER=deepseek

DEEPSEEK_API_KEY=sk-...
OPENAI_API_KEY=sk-proj-...

# Optional model overrides (defaults per provider in bench/common.py)
# REASONER_MODEL=deepseek-chat
# BROKER_MODEL=deepseek-chat
